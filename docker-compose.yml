# Compatibility entry point. The infrastructure Compose file is the canonical
# local stack; keeping this include lets existing `docker compose up` commands
# continue to work without mounting source over published containers.
include:
  - ./backend/infrastructure/docker-compose.yml
