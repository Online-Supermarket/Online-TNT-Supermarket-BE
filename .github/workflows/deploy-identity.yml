name: Deploy Identity API to Azure

on:
  workflow_run:
    workflows: [Release gate]
    types: [completed]

permissions:
  contents: read

concurrency:
  group: deploy-identity-production
  cancel-in-progress: false

jobs:
  deploy:
    if: >-
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.head_branch == 'main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event_name == 'workflow_run' && github.event.workflow_run.head_sha || github.sha }}

      - name: Log in to Azure Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.AZURE_ACR_LOGIN_SERVER }}
          username: ${{ secrets.AZURE_ACR_USERNAME }}
          password: ${{ secrets.AZURE_ACR_PASSWORD }}

      - name: Build and push image
        env:
          IMAGE: ${{ secrets.AZURE_ACR_LOGIN_SERVER }}/marketflow-identity:${{ github.event_name == 'workflow_run' && github.event.workflow_run.head_sha || github.sha }}
        run: |
          docker build --tag "$IMAGE" services/identity/Identity.Api
          docker push "$IMAGE"

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: tnt-supermarket-identity
          publish-profile: ${{ secrets.AZURE_IDENTITY_PUBLISH_PROFILE }}
          images: ${{ secrets.AZURE_ACR_LOGIN_SERVER }}/marketflow-identity:${{ github.event_name == 'workflow_run' && github.event.workflow_run.head_sha || github.sha }}

      - name: Verify readiness
        run: curl --fail --retry 12 --retry-all-errors --retry-delay 10 "https://tnt-supermarket-identity-b6dqd6hfgdf4esgz.indiasouthcentral-01.azurewebsites.net/health/ready"
